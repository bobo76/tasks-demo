open vs code to ~./claude

in task-client
  npm install
  npm start
  demo -> small bug
  npm test -> test faill

show plan.md
set to plan mode

# let's plan work in @plan.md. After exploring, stop and ask clarifying questions using AskUserQuestion tool before finalizing the plan. Once I approve, do the work in parallel where possible. After implementation, review code using skills and fix issues.
# let's plan work in @plan.md. After exploring, stop and ask clarifying questions before writing the final plan, at the end review code using skills and fix issues, when ready do work in parallel
# let's plan work in @plan.md, ask questions if needed, at the end review code using skills and fix issues, when ready do work in parallel

let's plan work in @plan.md.
After exploring, stop and ask clarifying questions using AskUserQuestion tool before finalizing the plan.
Once I approve, do the work in parallel where possible.
After implementation, review code using skills and fix issues.


/context

# Use skill interview on @plan.md, Write plan in : TECHNICAL_SPEC.md
# Follow @TECHNICAL_SPEC.md, do parallel work when possible, write progress in PROGRESS.md. Write reel start time, end time and total time per task

# Add a custom checkstyle.xml


netstat -ano | findstr :4300
netstat -ano | findstr :8090
taskkill //PID 30812 //F


Verification Commands

# Start backend
cd task-api && mvn spring-boot:run

# Start frontend (in another terminal)
cd task-client && npm start

# start backend
mvn spring-boot:run

# Run backend tests
cd task-api && mvn test

# Run frontend tests
cd task-client && npm test

# Run E2E tests (requires backend + frontend running)
cd task-e2e && npm test

# playwright only once
npx playwright install

Stop Condition:

- Backend: mvn clean package succeeds, jar runs on port 8090, sample data loads
- Frontend: npm run build succeeds, npm start runs on port 4300, UI renders
- Integration: Can view tasks in browser, create one task, drag between columns
- Cleanup: Stop all running processes (no background servers)
